apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acp.fullname" . }}-test-connection"
  labels:
    {{- include "acp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
