{{- if .Values.clientCredentials.create -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "acp-cd.clientCredentialsName" . }}
  labels:
    {{- include "acp-cd.labels" . | nindent 4 }}
stringData:
  importJobClientID: {{ .Values.clientCredentials.clientID }}
  importJobClientSecret: {{ .Values.clientCredentials.clientSecret }}
  importJobIssiuerURL: {{ .Values.clientCredentials.issiuerURL }}
{{- end }}
