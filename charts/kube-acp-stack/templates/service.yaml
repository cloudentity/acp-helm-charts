apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-acp-stack.fullname" . }}
  labels:
    {{- include "kube-acp-stack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-acp-stack.selectorLabels" . | nindent 4 }}
